@using TodoSeUsa.Infrastructure.Data
@using Microsoft.AspNetCore.Identity

@inject SignInManager<ApplicationUser> SignInManager

<ul class="nav nav-pills flex-column">
    <li class="nav-item">
        <NavLink class="nav-link" href="Cuenta/Administrar" Match="NavLinkMatch.All">Mi cuenta</NavLink>
    </li>
    <li class="nav-item">
        <NavLink class="nav-link" href="Cuenta/Administrar/CambiarContraseña">Cambiar mi contraseña</NavLink>
    </li>
</ul>

@code {
    private bool hasExternalLogins;

    protected override async Task OnInitializedAsync()
    {
        hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();
    }
}
